
name: Deploy to Firebase Hosting

# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
'on':
  push:
    branches:
      - main

  workflow_dispatch:
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
<<<<<<< HEAD


      - run: npm install -f
      - run: npm run build
      - name: Archive Build
        uses: actions/upload-artifact@v2
        with:
          name: build
          path: build

      - run: |
          npm install -f
          npm run build
=======
      - run: npm install -f
      - run: npm run build
>>>>>>> e2d4671 (chore(firebase-hosting-merge.yml): reformat file and remove unnecessary whitespace)
      - name: Archive Build
        uses: actions/upload-artifact@v2
        with:
          name: build
          path: build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PHOENIX_C88B9 }}'
          channelId: live
          projectId: phoenix-c88b9